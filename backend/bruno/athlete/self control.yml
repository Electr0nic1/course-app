info:
  name: self control
  type: http
  seq: 4

http:
  method: GET
  url: "{{api-host}}/athlete/self-controls"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
