info:
  name: trainings show status
  type: http
  seq: 8

http:
  method: PATCH
  url: "{{api-host}}/athlete/trainings/10/status"
  body:
    type: json
    data: |-
      {
        "status": "completed"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
