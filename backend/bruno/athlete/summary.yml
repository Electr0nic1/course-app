info:
  name: summary
  type: http
  seq: 9

http:
  method: GET
  url: "{{api-host}}/dashboard/summary"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
