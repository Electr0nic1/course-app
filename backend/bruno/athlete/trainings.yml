info:
  name: trainings
  type: http
  seq: 6

http:
  method: GET
  url: "{{api-host}}/athlete/trainings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
