info:
  name: self control show
  type: http
  seq: 3

http:
  method: GET
  url: "{{api-host}}/athlete/self-controls/121"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
