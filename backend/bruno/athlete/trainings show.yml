info:
  name: trainings show
  type: http
  seq: 7

http:
  method: GET
  url: "{{api-host}}/athlete/trainings/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
