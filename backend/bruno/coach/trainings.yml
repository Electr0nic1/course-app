info:
  name: trainings
  type: http
  seq: 4

http:
  method: GET
  url: "{{api-host}}/coach/trainings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
