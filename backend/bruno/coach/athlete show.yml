info:
  name: athlete show
  type: http
  seq: 2

http:
  method: GET
  url: "{{api-host}}/coach/athletes/9"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
