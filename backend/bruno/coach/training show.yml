info:
  name: training show
  type: http
  seq: 6

http:
  method: GET
  url: "{{api-host}}/coach/trainings/25"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
