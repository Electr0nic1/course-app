info:
  name: training delete
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{api-host}}/coach/trainings/25"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
