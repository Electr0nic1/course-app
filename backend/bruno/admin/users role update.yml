info:
  name: users role update
  type: http
  seq: 6

http:
  method: PUT
  url: "{{api-host}}/admin/users/18/role"
  body:
    type: json
    data: |
      {
        "role_id": 3
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
