info:
  name: users delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{api-host}}/admin/users/17"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
