info:
  name: users show
  type: http
  seq: 3

http:
  method: GET
  url: "{{api-host}}/admin/users/13"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
