info:
  name: "logout "
  type: http
  seq: 1

http:
  method: POST
  url: "{{api-host}}/auth/logout"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
